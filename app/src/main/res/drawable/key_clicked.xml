<?xml version="1.0" encoding="utf-8"?>

<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- color of the border of clicked key -->
    <stroke android:width="1.5dp"
            android:color="@color/keyboard_label_color" />

    <!-- color of clicked key -->
    <solid android:color="@color/keyboard_blue" />
</shape>